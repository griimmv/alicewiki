# AliceWiki

An AI agent that fetches information only from Wikipedia API.

---

# TODO
Make it so that its compatible for opencode zen provider

- i did change the opencode baseURL to its endpoint on the opencode zen docs, but it didn't work
 

